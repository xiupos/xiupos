---
import BaseHead from '../../components/BaseHead.astro';
import Header from '../../components/Header.astro';
import Footer from '../../components/Footer.astro';
import FormattedDate from '../../components/FormattedDate.astro';
import Social from '../../components/Social.astro';
import { BLOG_TITLE, SITE_AUTHOR, BLOG_DESCRIPTION, HEADER_LINKS } from '../../consts';
import { getCollection } from 'astro:content';

const posts = (await getCollection('blog')).sort(
  (a, b) => b.data.pubDate.valueOf() - a.data.pubDate.valueOf()
);

const rssLink = [{ link: 'https://xiupos.net/blog/rss.xml', icon: 'rss' }];
---

<!DOCTYPE html>
<html lang="en">
  <head>
    <BaseHead title={BLOG_TITLE} author={SITE_AUTHOR} description={BLOG_DESCRIPTION} />
    <style lang="stylus">
      ul
        box-sizing border-box
        padding 0
        list-style-type none

        li
          display flex
          margin-bottom .3em

          :global(time)
            flex 0 0 8em
            color var(--sub)
    </style>
  </head>
  <body>
    <Header links={HEADER_LINKS} />
    <main>
      <article>
        <ul>
          {
            posts.map((post) => (
              <li>
                <FormattedDate date={post.data.pubDate} />
                <a href={`/blog/${post.slug}/`}>{post.data.title}</a>
              </li>
            ))
          }
        </ul>
      </article>
      <Social links={rssLink} />
    </main>
    <Footer />
  </body>
</html>
